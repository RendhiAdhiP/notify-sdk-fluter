name: rws_sdk
description: RWS SDK — Realtime WebSocket client untuk notifikasi, chat, dan event realtime multi-tenant. Versi Flutter/Dart.
version: 2.0.0
publish_to: none

environment:
  sdk: ^3.2.0

dependencies:
  socket_io_client: ^3.0.0
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
